# /`reload`

> 在基岩版中重新加载行为包中的函数和脚本。

**加入版本：** 1.8.0

| 属性 | 值 |
|---|---|
| **别名** | `无` |
| **权限等级** | 2 |
| **需要作弊** | 是 |
| **额外说明** | 不在命令方块上执行 |

## 语法

```
reload [all: reload_all]
```

## 参数详解

- **`all`**:reload_all: enum - 必须为all，使玩家退出并重新加入世界，同时重载所有行为包和资源包。仅非BDS可用。
